<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/css/styles.css" rel="stylesheet">

    <title><%= title %></title>

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body class="bg-white min-h-screen font-[Comic_Sans_MS]">
    <div class="text-base text-gray-600 mb-4 px-10 pt-10">
      <a href="/" class="text-blue-600 underline hover:text-blue-800">Home</a> &gt; 
      <a href="/courses/<%= course.id %>/chapters" class="text-blue-600 underline hover:text-blue-800"><%= course.title %></a> &gt;
      <a href="/chapters/<%= chapter.id %>/pages" class="text-blue-600 underline hover:text-blue-800"><%= chapter.title %></a> &gt;
      New Page
    </div>
  
    <div class="p-10 pt-3">
      <h1 class="text-[3rem] font-bold mb-10">New Page</h1>
  
      <form method="POST" class="space-y-8">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
  
        <div>
          <label for="title" class="block text-gray-700 mb-4">Title</label>
          <input type="text" id="title" name="title" placeholder="Page heading" class="w-[1000px] border px-4 py-2 rounded" required>
        </div>
  
        <div>
          <label for="content" class="block text-gray-700 mb-4">Content</label>
          <textarea id="content" name="content" rows="10" placeholder="Detailed Content" class="w-[1000px] border px-4 py-2 rounded" required></textarea>
        </div>
  
        <div>
          <button type="submit" class="bg-black text-white py-2 px-8 rounded hover:bg-gray-800 font-semibold">
            Submit
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
