<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/css/styles.css" rel="stylesheet">

    <title><%= title %></title>

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body class="bg-gray-100 min-h-screen font-[Comic_Sans_MS] px-24 py-16 leading-relaxed">
    <%- include("breadcrumbs.ejs",{path: [`/courses/${course.id}/chapters`,course.title,"Create a new chapter"],isAuthor: true}) %>

    <h1 class="text-[3rem] font-bold mb-10">Create Chapter</h1>

    <form method="POST" class="space-y-8">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">

      <div>
        <label for="title" class="block text-gray-700 mb-4">Title</label>
        <input type="text" id="title" name="title" placeholder="Chapter name" class="bg-white w-[1000px] border px-4 py-2 rounded">
      </div>

      <div>
        <label for="description" class="block text-gray-700 mb-4">Description</label>
        <textarea id="description" name="description" rows="7" placeholder="A brief description for the chapter" class="bg-white w-[1000px] border px-4 py-2 rounded"></textarea>
      </div>

      <div>
        <button type="submit" class="bg-black text-white py-2 px-8 rounded hover:bg-gray-800 font-semibold">
          Submit
        </button>
      </div>
    </form>

    <%- include('notification.ejs') %>
  </body>
</html>